import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    marginBottom: 50,
    marginRight: 30,
    width: 120,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.8,
    shadowRadius: 2,
  },
});
